<isdecorate template="pt_csc.isml">
    <isif condition="${pdict.success}">
        <div class="alert alert-success">
            ${Resource.msg('label.cancel.success', 'bm', null)}
        </div>
    <iselse/>
        <div class="alert alert-danger">
            ${Resource.msg('label.cancel.failed', 'bm', null)} <isprint value="${pdict.errorMessage}" />
        </div>
    </isif>
</isdecorate>
